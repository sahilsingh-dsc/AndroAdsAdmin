<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tetraval.androadsadmin">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.activities.tv.TVIntentActivity" />
        <activity android:name=".ui.activities.tv.AddTVActivity" />
        <activity android:name=".ui.activities.tv.ManageTVActivity" />
        <activity android:name=".ui.activities.ads.NewAdsActivity" />
        <activity android:name=".ui.activities.ads.ManageAdsActivity" />
        <activity android:name=".ui.activities.controller.MainActivity" />
        <activity android:name=".ui.activities.auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>